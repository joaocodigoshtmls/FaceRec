# ===== DATABASE (MySQL via Prisma) =====
# Opção 1: DATABASE_URL completa
DATABASE_URL="mysql://user:password@localhost:3306/face_rec?serverVersion=8.0"

# Opção 2: Variáveis individuais (se DATABASE_URL não estiver definida)
DB_HOST=mysql-facerec.alwaysdata.net
DB_PORT=3306
DB_USER=facerec
DB_PASSWORD=iqmi8j55PDpHQ
DB_NAME=facerec_1

# ===== CORS - Origens permitidas =====
# Formato: URLs separadas por vírgula, sem espaços extras
# Localhost é habilitado por padrão. Adicione aqui domínios de produção
CORS_ORIGINS="https://app.vercel.app,https://seu-dominio.com,http://localhost:8080"

# ===== FRONTEND - URL absoluta do backend =====
# Para Vercel + AlwaysData: especifique URL absoluta do backend
# Se vazio, usa /api (mesma origin)
VITE_API_URL="https://seu-backend.alwaysdata.com/api"

# Alternativamente, para desenvolvimento local:
# VITE_API_URL="http://localhost:3001/api"
# Ou deixe vazio para usar URLs relativas:
# VITE_API_URL=""

# ===== OUTRAS VARIÁVEIS =====
VITE_CAM_BASE=http://localhost:4000
VITE_SOCKET_BASE=http://localhost:4000
VITE_CLASSROOM_CODE=3AT.I
VITE_CAM_STREAM_URL=http://localhost:8080/stream
PORT=4000

# ===== DEBUG =====
DEBUG_API=0  # Mude para 1 em desenvolvimento para ver detalhes de erro

# ===== JWT =====
JWT_SECRET=G4n!$k2@rQ9vXyZpL8wH^t3jUeB7m
JWT_EXPIRES_IN=24h

# ===== RUNTIME (Vercel) =====
# Força Node.js runtime (necessário para bcryptjs)
# Configure em vercel.json: "runtime": "nodejs20.x"