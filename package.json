{
  "name": "facerec-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend",
    "build:all": "npm run prisma:generate --workspace backend && npm run build --workspace frontend",
  "vercel-build": "cd api && npm install --no-audit --no-fund && cd ../frontend && npm install --no-audit --no-fund && npm run build",
    "lint": "npm run lint --workspace frontend",
    "preview": "npm run preview --workspace frontend"
  }
}
