# ==========================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE PARA PRODUÇÃO
# ==========================================
# 
# Este arquivo serve como referência. Não commite arquivos .env com dados reais!
# Configure estas variáveis nas respectivas plataformas:
# - Netlify: Site settings > Build & deploy > Environment
# - Railway: Service > Variables

# ==========================================
# FRONTEND - NETLIFY
# ==========================================
# Configurar no painel do Netlify em: Site settings > Environment variables

# URL do backend em produção (Railway)
VITE_API_URL=https://facerec-backend.up.railway.app/api

# URL base para câmera
VITE_CAM_BASE=https://facerec-backend.up.railway.app

# URL base para WebSocket
VITE_SOCKET_BASE=https://facerec-backend.up.railway.app

# Código da sala de aula (ajuste conforme necessário)
VITE_CLASSROOM_CODE=3AT.I

# URL do stream da câmera (se necessário)
VITE_CAM_STREAM_URL=http://localhost:8080/stream

# ==========================================
# BACKEND - RAILWAY
# ==========================================
# Configurar no painel do Railway em: Service > Variables

# ===== DATABASE (MySQL via AlwaysData) =====
# STRING DE CONEXÃO COMPLETA - COPIAR DO PAINEL DO ALWAYSDATA
DATABASE_URL="mysql://facerec:iqmi8j55PDpHQ@mysql-facerec.alwaysdata.net:3306/facerec_1"

# OU usar variáveis individuais (não recomendado se usar DATABASE_URL)
# DB_HOST=mysql-facerec.alwaysdata.net
# DB_PORT=3306
# DB_USER=facerec
# DB_PASSWORD=iqmi8j55PDpHQ
# DB_NAME=facerec_1

# ===== PORTA (Railway fornece automaticamente) =====
# O Railway define PORT automaticamente, não é necessário configurar
# PORT será injetado automaticamente pelo Railway

# ===== CORS - Origens permitidas =====
# URLs que podem acessar a API
# Substituir pela URL real do Netlify após o deploy
CORS_ORIGINS="https://facerec.netlify.app,https://seu-dominio-custom.com"

# ===== JWT =====
# Chave secreta para tokens JWT (GERAR UMA NOVA E SEGURA!)
# Use: openssl rand -base64 32
JWT_SECRET=GERE_UMA_CHAVE_SECRETA_AQUI_COM_OPENSSL
JWT_EXPIRES_IN=24h

# ===== ADMIN PADRÃO =====
# Credenciais do administrador padrão (altere em produção!)
DEFAULT_ADMIN_LOGIN=@administrador
DEFAULT_ADMIN_PASSWORD=TROQUE_ESTA_SENHA_EM_PRODUCAO
DEFAULT_ADMIN_ID=predefined-admin
DEFAULT_ADMIN_NAME=Administrador do Sistema

# ===== DEBUG =====
# Desabilitar em produção (0 ou remover)
# DEBUG_API=0

# ===== NODE ENV =====
NODE_ENV=production

# ==========================================
# BANCO DE DADOS - ALWAYSDATA
# ==========================================
# Configurar no painel do AlwaysData:
# 1. Acesse Databases > MySQL
# 2. Anote as credenciais:
#    - Host: mysql-facerec.alwaysdata.net
#    - Port: 3306
#    - Database: facerec_1
#    - User: facerec
#    - Password: (sua senha)
# 3. Monte a DATABASE_URL no formato:
#    mysql://USER:PASSWORD@HOST:PORT/DATABASE
# 4. Cole no Railway

# ==========================================
# CHECKLIST DE SEGURANÇA
# ==========================================
# [ ] JWT_SECRET foi alterado para um valor único e seguro
# [ ] DEFAULT_ADMIN_PASSWORD foi alterado
# [ ] CORS_ORIGINS contém apenas domínios autorizados
# [ ] DATABASE_URL não está commitada no git
# [ ] .env.production está no .gitignore
# [ ] Variáveis foram configuradas no Netlify
# [ ] Variáveis foram configuradas no Railway
# [ ] Conexão com banco AlwaysData foi testada
